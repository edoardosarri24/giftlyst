services:
  frontend:
    container_name: frontend
    image: ghcr.io/edoardosarri24/regalamelo-frontend:main
    restart: always
    env_file:
      - nas.env
    environment:
      - VITE_API_BASE_URL=https://regalamelo.edoardosarri.com/api/v1
    depends_on:
      - api
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  api:
    container_name: api
    image: ghcr.io/edoardosarri24/regalamelo-backend:main
    restart: always
    command: sh -c "if npx prisma migrate deploy; then echo 'Migrations up to date'; else echo 'Deploy failed, trying to baseline 0_init...' && npx prisma migrate resolve --applied 0_init && npx prisma migrate deploy; fi && exec node dist/index.js"
    env_file:
      - nas.env
    depends_on:
      db:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  db:
    image: postgres:17-alpine
    restart: always
    volumes:
      - NAS_DATABASE_DATA:/var/lib/postgresql/data
    env_file:
      - nas.env
    environment:
      - PGDATA=/var/lib/postgresql/data/pg
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U giftlistuser -d giftlistdb"]
      interval: 5s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  tunnel:
    image: cloudflare/cloudflared:latest
    restart: always
    command: tunnel run
    env_file:
      - nas.env
    depends_on:
      - frontend
      - api

  watchtower:
    image: containrrr/watchtower
    restart: always
    environment:
      - DOCKER_API_VERSION=1.40
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 300 frontend api

  db-backup:
    image: prodrigestivill/postgres-backup-local
    restart: always
    volumes:
      - NAS_DATABASE_DATA:/var/lib/postgresql/data:ro
      - ./backups:/backups
    env_file:
      - nas.env
    environment:
      - POSTGRES_HOST=db
      - SCHEDULE=@daily
      - BACKUP_KEEP_DAYS=7
    depends_on:
      - db

volumes:
  NAS_DATABASE_DATA:
